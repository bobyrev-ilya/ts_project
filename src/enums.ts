enum Status{
    Error,
    Ok,
    Warning
}

const error = Status.Error; // 0
const ok = Status[1]; // OK

enum SocialMedia{
    VK = "VK",
    FACEBOOK = "FACEBOOK"
}

const social = SocialMedia.FACEBOOK; // FACEBOOK
